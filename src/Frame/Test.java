package Frame;

public class Test {
	public static void main(String args[]) {
		//On initialise notre classe SearchRes qui est l'interface de recherche
		SearchRes search = new SearchRes();
		//setVisible sert à visualiser l'interface concernée
		search.setVisible(true);
		
	}
}
